# =============================================================================
# Telegram Bot Configuration
# =============================================================================

# Bot token from @BotFather (required)
# Format: 123456789:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
BOT_TOKEN=your_bot_token_here

# Secret token for webhook validation (required)
# Generate with: openssl rand -hex 32
# Only A-Z, a-z, 0-9, _ and - are allowed (1-256 chars)
TG_WEBHOOK_SECRET=your_webhook_secret_here

# =============================================================================
# Webhook Registration (used by scripts/set_webhook.py)
# =============================================================================

# Public HTTPS URL where Telegram will send updates (required for registration)
# For local dev, use ngrok: https://abc123.ngrok.io/telegram/webhook
WEBHOOK_URL=https://your-domain.com/telegram/webhook

# Maximum concurrent connections from Telegram (1-100, default: 40)
# WEBHOOK_MAX_CONNECTIONS=40

# Drop pending updates when setting webhook (default: true)
# WEBHOOK_DROP_PENDING=true

# =============================================================================
# Application Settings
# =============================================================================

# Logging level: debug, info, warning, error (default: info)
LOG_LEVEL=info

# Output handler: stdout, noop (default: stdout)
OUTPUT_HANDLER=stdout

# Server binding
HOST=0.0.0.0
PORT=8000
